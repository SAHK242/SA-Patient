// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"patient/ent/inpatientdetail"
	"patient/ent/predicate"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// InpatientDetailQuery is the builder for querying InpatientDetail entities.
type InpatientDetailQuery struct {
	config
	ctx        *QueryContext
	order      []inpatientdetail.OrderOption
	inters     []Interceptor
	predicates []predicate.InpatientDetail
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the InpatientDetailQuery builder.
func (idq *InpatientDetailQuery) Where(ps ...predicate.InpatientDetail) *InpatientDetailQuery {
	idq.predicates = append(idq.predicates, ps...)
	return idq
}

// Limit the number of records to be returned by this query.
func (idq *InpatientDetailQuery) Limit(limit int) *InpatientDetailQuery {
	idq.ctx.Limit = &limit
	return idq
}

// Offset to start from.
func (idq *InpatientDetailQuery) Offset(offset int) *InpatientDetailQuery {
	idq.ctx.Offset = &offset
	return idq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (idq *InpatientDetailQuery) Unique(unique bool) *InpatientDetailQuery {
	idq.ctx.Unique = &unique
	return idq
}

// Order specifies how the records should be ordered.
func (idq *InpatientDetailQuery) Order(o ...inpatientdetail.OrderOption) *InpatientDetailQuery {
	idq.order = append(idq.order, o...)
	return idq
}

// First returns the first InpatientDetail entity from the query.
// Returns a *NotFoundError when no InpatientDetail was found.
func (idq *InpatientDetailQuery) First(ctx context.Context) (*InpatientDetail, error) {
	nodes, err := idq.Limit(1).All(setContextOp(ctx, idq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{inpatientdetail.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (idq *InpatientDetailQuery) FirstX(ctx context.Context) *InpatientDetail {
	node, err := idq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first InpatientDetail ID from the query.
// Returns a *NotFoundError when no InpatientDetail ID was found.
func (idq *InpatientDetailQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = idq.Limit(1).IDs(setContextOp(ctx, idq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{inpatientdetail.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (idq *InpatientDetailQuery) FirstIDX(ctx context.Context) int {
	id, err := idq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single InpatientDetail entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one InpatientDetail entity is found.
// Returns a *NotFoundError when no InpatientDetail entities are found.
func (idq *InpatientDetailQuery) Only(ctx context.Context) (*InpatientDetail, error) {
	nodes, err := idq.Limit(2).All(setContextOp(ctx, idq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{inpatientdetail.Label}
	default:
		return nil, &NotSingularError{inpatientdetail.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (idq *InpatientDetailQuery) OnlyX(ctx context.Context) *InpatientDetail {
	node, err := idq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only InpatientDetail ID in the query.
// Returns a *NotSingularError when more than one InpatientDetail ID is found.
// Returns a *NotFoundError when no entities are found.
func (idq *InpatientDetailQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = idq.Limit(2).IDs(setContextOp(ctx, idq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{inpatientdetail.Label}
	default:
		err = &NotSingularError{inpatientdetail.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (idq *InpatientDetailQuery) OnlyIDX(ctx context.Context) int {
	id, err := idq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of InpatientDetails.
func (idq *InpatientDetailQuery) All(ctx context.Context) ([]*InpatientDetail, error) {
	ctx = setContextOp(ctx, idq.ctx, ent.OpQueryAll)
	if err := idq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*InpatientDetail, *InpatientDetailQuery]()
	return withInterceptors[[]*InpatientDetail](ctx, idq, qr, idq.inters)
}

// AllX is like All, but panics if an error occurs.
func (idq *InpatientDetailQuery) AllX(ctx context.Context) []*InpatientDetail {
	nodes, err := idq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of InpatientDetail IDs.
func (idq *InpatientDetailQuery) IDs(ctx context.Context) (ids []int, err error) {
	if idq.ctx.Unique == nil && idq.path != nil {
		idq.Unique(true)
	}
	ctx = setContextOp(ctx, idq.ctx, ent.OpQueryIDs)
	if err = idq.Select(inpatientdetail.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (idq *InpatientDetailQuery) IDsX(ctx context.Context) []int {
	ids, err := idq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (idq *InpatientDetailQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, idq.ctx, ent.OpQueryCount)
	if err := idq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, idq, querierCount[*InpatientDetailQuery](), idq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (idq *InpatientDetailQuery) CountX(ctx context.Context) int {
	count, err := idq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (idq *InpatientDetailQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, idq.ctx, ent.OpQueryExist)
	switch _, err := idq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (idq *InpatientDetailQuery) ExistX(ctx context.Context) bool {
	exist, err := idq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the InpatientDetailQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (idq *InpatientDetailQuery) Clone() *InpatientDetailQuery {
	if idq == nil {
		return nil
	}
	return &InpatientDetailQuery{
		config:     idq.config,
		ctx:        idq.ctx.Clone(),
		order:      append([]inpatientdetail.OrderOption{}, idq.order...),
		inters:     append([]Interceptor{}, idq.inters...),
		predicates: append([]predicate.InpatientDetail{}, idq.predicates...),
		// clone intermediate query.
		sql:  idq.sql.Clone(),
		path: idq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
func (idq *InpatientDetailQuery) GroupBy(field string, fields ...string) *InpatientDetailGroupBy {
	idq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &InpatientDetailGroupBy{build: idq}
	grbuild.flds = &idq.ctx.Fields
	grbuild.label = inpatientdetail.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
func (idq *InpatientDetailQuery) Select(fields ...string) *InpatientDetailSelect {
	idq.ctx.Fields = append(idq.ctx.Fields, fields...)
	sbuild := &InpatientDetailSelect{InpatientDetailQuery: idq}
	sbuild.label = inpatientdetail.Label
	sbuild.flds, sbuild.scan = &idq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a InpatientDetailSelect configured with the given aggregations.
func (idq *InpatientDetailQuery) Aggregate(fns ...AggregateFunc) *InpatientDetailSelect {
	return idq.Select().Aggregate(fns...)
}

func (idq *InpatientDetailQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range idq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, idq); err != nil {
				return err
			}
		}
	}
	for _, f := range idq.ctx.Fields {
		if !inpatientdetail.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if idq.path != nil {
		prev, err := idq.path(ctx)
		if err != nil {
			return err
		}
		idq.sql = prev
	}
	return nil
}

func (idq *InpatientDetailQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*InpatientDetail, error) {
	var (
		nodes = []*InpatientDetail{}
		_spec = idq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*InpatientDetail).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &InpatientDetail{config: idq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, idq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (idq *InpatientDetailQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := idq.querySpec()
	_spec.Node.Columns = idq.ctx.Fields
	if len(idq.ctx.Fields) > 0 {
		_spec.Unique = idq.ctx.Unique != nil && *idq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, idq.driver, _spec)
}

func (idq *InpatientDetailQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(inpatientdetail.Table, inpatientdetail.Columns, sqlgraph.NewFieldSpec(inpatientdetail.FieldID, field.TypeInt))
	_spec.From = idq.sql
	if unique := idq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if idq.path != nil {
		_spec.Unique = true
	}
	if fields := idq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, inpatientdetail.FieldID)
		for i := range fields {
			if fields[i] != inpatientdetail.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := idq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := idq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := idq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := idq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (idq *InpatientDetailQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(idq.driver.Dialect())
	t1 := builder.Table(inpatientdetail.Table)
	columns := idq.ctx.Fields
	if len(columns) == 0 {
		columns = inpatientdetail.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if idq.sql != nil {
		selector = idq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if idq.ctx.Unique != nil && *idq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range idq.predicates {
		p(selector)
	}
	for _, p := range idq.order {
		p(selector)
	}
	if offset := idq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := idq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// InpatientDetailGroupBy is the group-by builder for InpatientDetail entities.
type InpatientDetailGroupBy struct {
	selector
	build *InpatientDetailQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (idgb *InpatientDetailGroupBy) Aggregate(fns ...AggregateFunc) *InpatientDetailGroupBy {
	idgb.fns = append(idgb.fns, fns...)
	return idgb
}

// Scan applies the selector query and scans the result into the given value.
func (idgb *InpatientDetailGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, idgb.build.ctx, ent.OpQueryGroupBy)
	if err := idgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*InpatientDetailQuery, *InpatientDetailGroupBy](ctx, idgb.build, idgb, idgb.build.inters, v)
}

func (idgb *InpatientDetailGroupBy) sqlScan(ctx context.Context, root *InpatientDetailQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(idgb.fns))
	for _, fn := range idgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*idgb.flds)+len(idgb.fns))
		for _, f := range *idgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*idgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := idgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// InpatientDetailSelect is the builder for selecting fields of InpatientDetail entities.
type InpatientDetailSelect struct {
	*InpatientDetailQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ids *InpatientDetailSelect) Aggregate(fns ...AggregateFunc) *InpatientDetailSelect {
	ids.fns = append(ids.fns, fns...)
	return ids
}

// Scan applies the selector query and scans the result into the given value.
func (ids *InpatientDetailSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ids.ctx, ent.OpQuerySelect)
	if err := ids.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*InpatientDetailQuery, *InpatientDetailSelect](ctx, ids.InpatientDetailQuery, ids, ids.inters, v)
}

func (ids *InpatientDetailSelect) sqlScan(ctx context.Context, root *InpatientDetailQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ids.fns))
	for _, fn := range ids.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ids.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ids.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
