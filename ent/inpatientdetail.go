// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"patient/ent/inpatientdetail"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// InpatientDetail is the model entity for the InpatientDetail schema.
type InpatientDetail struct {
	config
	// ID of the ent.
	ID           int `json:"id,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*InpatientDetail) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case inpatientdetail.FieldID:
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the InpatientDetail fields.
func (id *InpatientDetail) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case inpatientdetail.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			id.ID = int(value.Int64)
		default:
			id.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the InpatientDetail.
// This includes values selected through modifiers, order, etc.
func (id *InpatientDetail) Value(name string) (ent.Value, error) {
	return id.selectValues.Get(name)
}

// Update returns a builder for updating this InpatientDetail.
// Note that you need to call InpatientDetail.Unwrap() before calling this method if this InpatientDetail
// was returned from a transaction, and the transaction was committed or rolled back.
func (id *InpatientDetail) Update() *InpatientDetailUpdateOne {
	return NewInpatientDetailClient(id.config).UpdateOne(id)
}

// Unwrap unwraps the InpatientDetail entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (id *InpatientDetail) Unwrap() *InpatientDetail {
	_tx, ok := id.config.driver.(*txDriver)
	if !ok {
		panic("ent: InpatientDetail is not a transactional entity")
	}
	id.config.driver = _tx.drv
	return id
}

// String implements the fmt.Stringer.
func (id *InpatientDetail) String() string {
	var builder strings.Builder
	builder.WriteString("InpatientDetail(")
	builder.WriteString(fmt.Sprintf("id=%v", id.ID))
	builder.WriteByte(')')
	return builder.String()
}

// InpatientDetails is a parsable slice of InpatientDetail.
type InpatientDetails []*InpatientDetail
