// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"patient/ent/medicalhistories"
	"patient/ent/medicalsurgery"
	"patient/ent/predicate"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// MedicalSurgeryQuery is the builder for querying MedicalSurgery entities.
type MedicalSurgeryQuery struct {
	config
	ctx                  *QueryContext
	order                []medicalsurgery.OrderOption
	inters               []Interceptor
	predicates           []predicate.MedicalSurgery
	withMedicalHistories *MedicalHistoriesQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the MedicalSurgeryQuery builder.
func (msq *MedicalSurgeryQuery) Where(ps ...predicate.MedicalSurgery) *MedicalSurgeryQuery {
	msq.predicates = append(msq.predicates, ps...)
	return msq
}

// Limit the number of records to be returned by this query.
func (msq *MedicalSurgeryQuery) Limit(limit int) *MedicalSurgeryQuery {
	msq.ctx.Limit = &limit
	return msq
}

// Offset to start from.
func (msq *MedicalSurgeryQuery) Offset(offset int) *MedicalSurgeryQuery {
	msq.ctx.Offset = &offset
	return msq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (msq *MedicalSurgeryQuery) Unique(unique bool) *MedicalSurgeryQuery {
	msq.ctx.Unique = &unique
	return msq
}

// Order specifies how the records should be ordered.
func (msq *MedicalSurgeryQuery) Order(o ...medicalsurgery.OrderOption) *MedicalSurgeryQuery {
	msq.order = append(msq.order, o...)
	return msq
}

// QueryMedicalHistories chains the current query on the "medical_histories" edge.
func (msq *MedicalSurgeryQuery) QueryMedicalHistories() *MedicalHistoriesQuery {
	query := (&MedicalHistoriesClient{config: msq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := msq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := msq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(medicalsurgery.Table, medicalsurgery.FieldID, selector),
			sqlgraph.To(medicalhistories.Table, medicalhistories.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, medicalsurgery.MedicalHistoriesTable, medicalsurgery.MedicalHistoriesColumn),
		)
		fromU = sqlgraph.SetNeighbors(msq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first MedicalSurgery entity from the query.
// Returns a *NotFoundError when no MedicalSurgery was found.
func (msq *MedicalSurgeryQuery) First(ctx context.Context) (*MedicalSurgery, error) {
	nodes, err := msq.Limit(1).All(setContextOp(ctx, msq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{medicalsurgery.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (msq *MedicalSurgeryQuery) FirstX(ctx context.Context) *MedicalSurgery {
	node, err := msq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first MedicalSurgery ID from the query.
// Returns a *NotFoundError when no MedicalSurgery ID was found.
func (msq *MedicalSurgeryQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = msq.Limit(1).IDs(setContextOp(ctx, msq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{medicalsurgery.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (msq *MedicalSurgeryQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := msq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single MedicalSurgery entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one MedicalSurgery entity is found.
// Returns a *NotFoundError when no MedicalSurgery entities are found.
func (msq *MedicalSurgeryQuery) Only(ctx context.Context) (*MedicalSurgery, error) {
	nodes, err := msq.Limit(2).All(setContextOp(ctx, msq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{medicalsurgery.Label}
	default:
		return nil, &NotSingularError{medicalsurgery.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (msq *MedicalSurgeryQuery) OnlyX(ctx context.Context) *MedicalSurgery {
	node, err := msq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only MedicalSurgery ID in the query.
// Returns a *NotSingularError when more than one MedicalSurgery ID is found.
// Returns a *NotFoundError when no entities are found.
func (msq *MedicalSurgeryQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = msq.Limit(2).IDs(setContextOp(ctx, msq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{medicalsurgery.Label}
	default:
		err = &NotSingularError{medicalsurgery.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (msq *MedicalSurgeryQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := msq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of MedicalSurgeries.
func (msq *MedicalSurgeryQuery) All(ctx context.Context) ([]*MedicalSurgery, error) {
	ctx = setContextOp(ctx, msq.ctx, ent.OpQueryAll)
	if err := msq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*MedicalSurgery, *MedicalSurgeryQuery]()
	return withInterceptors[[]*MedicalSurgery](ctx, msq, qr, msq.inters)
}

// AllX is like All, but panics if an error occurs.
func (msq *MedicalSurgeryQuery) AllX(ctx context.Context) []*MedicalSurgery {
	nodes, err := msq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of MedicalSurgery IDs.
func (msq *MedicalSurgeryQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if msq.ctx.Unique == nil && msq.path != nil {
		msq.Unique(true)
	}
	ctx = setContextOp(ctx, msq.ctx, ent.OpQueryIDs)
	if err = msq.Select(medicalsurgery.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (msq *MedicalSurgeryQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := msq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (msq *MedicalSurgeryQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, msq.ctx, ent.OpQueryCount)
	if err := msq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, msq, querierCount[*MedicalSurgeryQuery](), msq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (msq *MedicalSurgeryQuery) CountX(ctx context.Context) int {
	count, err := msq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (msq *MedicalSurgeryQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, msq.ctx, ent.OpQueryExist)
	switch _, err := msq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (msq *MedicalSurgeryQuery) ExistX(ctx context.Context) bool {
	exist, err := msq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the MedicalSurgeryQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (msq *MedicalSurgeryQuery) Clone() *MedicalSurgeryQuery {
	if msq == nil {
		return nil
	}
	return &MedicalSurgeryQuery{
		config:               msq.config,
		ctx:                  msq.ctx.Clone(),
		order:                append([]medicalsurgery.OrderOption{}, msq.order...),
		inters:               append([]Interceptor{}, msq.inters...),
		predicates:           append([]predicate.MedicalSurgery{}, msq.predicates...),
		withMedicalHistories: msq.withMedicalHistories.Clone(),
		// clone intermediate query.
		sql:  msq.sql.Clone(),
		path: msq.path,
	}
}

// WithMedicalHistories tells the query-builder to eager-load the nodes that are connected to
// the "medical_histories" edge. The optional arguments are used to configure the query builder of the edge.
func (msq *MedicalSurgeryQuery) WithMedicalHistories(opts ...func(*MedicalHistoriesQuery)) *MedicalSurgeryQuery {
	query := (&MedicalHistoriesClient{config: msq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	msq.withMedicalHistories = query
	return msq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		MedicalHistoryID uuid.UUID `json:"medical_history_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.MedicalSurgery.Query().
//		GroupBy(medicalsurgery.FieldMedicalHistoryID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (msq *MedicalSurgeryQuery) GroupBy(field string, fields ...string) *MedicalSurgeryGroupBy {
	msq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &MedicalSurgeryGroupBy{build: msq}
	grbuild.flds = &msq.ctx.Fields
	grbuild.label = medicalsurgery.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		MedicalHistoryID uuid.UUID `json:"medical_history_id,omitempty"`
//	}
//
//	client.MedicalSurgery.Query().
//		Select(medicalsurgery.FieldMedicalHistoryID).
//		Scan(ctx, &v)
func (msq *MedicalSurgeryQuery) Select(fields ...string) *MedicalSurgerySelect {
	msq.ctx.Fields = append(msq.ctx.Fields, fields...)
	sbuild := &MedicalSurgerySelect{MedicalSurgeryQuery: msq}
	sbuild.label = medicalsurgery.Label
	sbuild.flds, sbuild.scan = &msq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a MedicalSurgerySelect configured with the given aggregations.
func (msq *MedicalSurgeryQuery) Aggregate(fns ...AggregateFunc) *MedicalSurgerySelect {
	return msq.Select().Aggregate(fns...)
}

func (msq *MedicalSurgeryQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range msq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, msq); err != nil {
				return err
			}
		}
	}
	for _, f := range msq.ctx.Fields {
		if !medicalsurgery.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if msq.path != nil {
		prev, err := msq.path(ctx)
		if err != nil {
			return err
		}
		msq.sql = prev
	}
	return nil
}

func (msq *MedicalSurgeryQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*MedicalSurgery, error) {
	var (
		nodes       = []*MedicalSurgery{}
		_spec       = msq.querySpec()
		loadedTypes = [1]bool{
			msq.withMedicalHistories != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*MedicalSurgery).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &MedicalSurgery{config: msq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, msq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := msq.withMedicalHistories; query != nil {
		if err := msq.loadMedicalHistories(ctx, query, nodes, nil,
			func(n *MedicalSurgery, e *MedicalHistories) { n.Edges.MedicalHistories = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (msq *MedicalSurgeryQuery) loadMedicalHistories(ctx context.Context, query *MedicalHistoriesQuery, nodes []*MedicalSurgery, init func(*MedicalSurgery), assign func(*MedicalSurgery, *MedicalHistories)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*MedicalSurgery)
	for i := range nodes {
		fk := nodes[i].MedicalHistoryID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(medicalhistories.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "medical_history_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (msq *MedicalSurgeryQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := msq.querySpec()
	_spec.Node.Columns = msq.ctx.Fields
	if len(msq.ctx.Fields) > 0 {
		_spec.Unique = msq.ctx.Unique != nil && *msq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, msq.driver, _spec)
}

func (msq *MedicalSurgeryQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(medicalsurgery.Table, medicalsurgery.Columns, sqlgraph.NewFieldSpec(medicalsurgery.FieldID, field.TypeUUID))
	_spec.From = msq.sql
	if unique := msq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if msq.path != nil {
		_spec.Unique = true
	}
	if fields := msq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, medicalsurgery.FieldID)
		for i := range fields {
			if fields[i] != medicalsurgery.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if msq.withMedicalHistories != nil {
			_spec.Node.AddColumnOnce(medicalsurgery.FieldMedicalHistoryID)
		}
	}
	if ps := msq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := msq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := msq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := msq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (msq *MedicalSurgeryQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(msq.driver.Dialect())
	t1 := builder.Table(medicalsurgery.Table)
	columns := msq.ctx.Fields
	if len(columns) == 0 {
		columns = medicalsurgery.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if msq.sql != nil {
		selector = msq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if msq.ctx.Unique != nil && *msq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range msq.predicates {
		p(selector)
	}
	for _, p := range msq.order {
		p(selector)
	}
	if offset := msq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := msq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// MedicalSurgeryGroupBy is the group-by builder for MedicalSurgery entities.
type MedicalSurgeryGroupBy struct {
	selector
	build *MedicalSurgeryQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (msgb *MedicalSurgeryGroupBy) Aggregate(fns ...AggregateFunc) *MedicalSurgeryGroupBy {
	msgb.fns = append(msgb.fns, fns...)
	return msgb
}

// Scan applies the selector query and scans the result into the given value.
func (msgb *MedicalSurgeryGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, msgb.build.ctx, ent.OpQueryGroupBy)
	if err := msgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MedicalSurgeryQuery, *MedicalSurgeryGroupBy](ctx, msgb.build, msgb, msgb.build.inters, v)
}

func (msgb *MedicalSurgeryGroupBy) sqlScan(ctx context.Context, root *MedicalSurgeryQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(msgb.fns))
	for _, fn := range msgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*msgb.flds)+len(msgb.fns))
		for _, f := range *msgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*msgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := msgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// MedicalSurgerySelect is the builder for selecting fields of MedicalSurgery entities.
type MedicalSurgerySelect struct {
	*MedicalSurgeryQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (mss *MedicalSurgerySelect) Aggregate(fns ...AggregateFunc) *MedicalSurgerySelect {
	mss.fns = append(mss.fns, fns...)
	return mss
}

// Scan applies the selector query and scans the result into the given value.
func (mss *MedicalSurgerySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mss.ctx, ent.OpQuerySelect)
	if err := mss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MedicalSurgeryQuery, *MedicalSurgerySelect](ctx, mss.MedicalSurgeryQuery, mss, mss.inters, v)
}

func (mss *MedicalSurgerySelect) sqlScan(ctx context.Context, root *MedicalSurgeryQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(mss.fns))
	for _, fn := range mss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*mss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
