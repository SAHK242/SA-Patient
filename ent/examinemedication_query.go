// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"patient/ent/examinemedication"
	"patient/ent/predicate"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ExamineMedicationQuery is the builder for querying ExamineMedication entities.
type ExamineMedicationQuery struct {
	config
	ctx        *QueryContext
	order      []examinemedication.OrderOption
	inters     []Interceptor
	predicates []predicate.ExamineMedication
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ExamineMedicationQuery builder.
func (emq *ExamineMedicationQuery) Where(ps ...predicate.ExamineMedication) *ExamineMedicationQuery {
	emq.predicates = append(emq.predicates, ps...)
	return emq
}

// Limit the number of records to be returned by this query.
func (emq *ExamineMedicationQuery) Limit(limit int) *ExamineMedicationQuery {
	emq.ctx.Limit = &limit
	return emq
}

// Offset to start from.
func (emq *ExamineMedicationQuery) Offset(offset int) *ExamineMedicationQuery {
	emq.ctx.Offset = &offset
	return emq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (emq *ExamineMedicationQuery) Unique(unique bool) *ExamineMedicationQuery {
	emq.ctx.Unique = &unique
	return emq
}

// Order specifies how the records should be ordered.
func (emq *ExamineMedicationQuery) Order(o ...examinemedication.OrderOption) *ExamineMedicationQuery {
	emq.order = append(emq.order, o...)
	return emq
}

// First returns the first ExamineMedication entity from the query.
// Returns a *NotFoundError when no ExamineMedication was found.
func (emq *ExamineMedicationQuery) First(ctx context.Context) (*ExamineMedication, error) {
	nodes, err := emq.Limit(1).All(setContextOp(ctx, emq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{examinemedication.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (emq *ExamineMedicationQuery) FirstX(ctx context.Context) *ExamineMedication {
	node, err := emq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ExamineMedication ID from the query.
// Returns a *NotFoundError when no ExamineMedication ID was found.
func (emq *ExamineMedicationQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = emq.Limit(1).IDs(setContextOp(ctx, emq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{examinemedication.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (emq *ExamineMedicationQuery) FirstIDX(ctx context.Context) int {
	id, err := emq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ExamineMedication entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ExamineMedication entity is found.
// Returns a *NotFoundError when no ExamineMedication entities are found.
func (emq *ExamineMedicationQuery) Only(ctx context.Context) (*ExamineMedication, error) {
	nodes, err := emq.Limit(2).All(setContextOp(ctx, emq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{examinemedication.Label}
	default:
		return nil, &NotSingularError{examinemedication.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (emq *ExamineMedicationQuery) OnlyX(ctx context.Context) *ExamineMedication {
	node, err := emq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ExamineMedication ID in the query.
// Returns a *NotSingularError when more than one ExamineMedication ID is found.
// Returns a *NotFoundError when no entities are found.
func (emq *ExamineMedicationQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = emq.Limit(2).IDs(setContextOp(ctx, emq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{examinemedication.Label}
	default:
		err = &NotSingularError{examinemedication.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (emq *ExamineMedicationQuery) OnlyIDX(ctx context.Context) int {
	id, err := emq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ExamineMedications.
func (emq *ExamineMedicationQuery) All(ctx context.Context) ([]*ExamineMedication, error) {
	ctx = setContextOp(ctx, emq.ctx, ent.OpQueryAll)
	if err := emq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ExamineMedication, *ExamineMedicationQuery]()
	return withInterceptors[[]*ExamineMedication](ctx, emq, qr, emq.inters)
}

// AllX is like All, but panics if an error occurs.
func (emq *ExamineMedicationQuery) AllX(ctx context.Context) []*ExamineMedication {
	nodes, err := emq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ExamineMedication IDs.
func (emq *ExamineMedicationQuery) IDs(ctx context.Context) (ids []int, err error) {
	if emq.ctx.Unique == nil && emq.path != nil {
		emq.Unique(true)
	}
	ctx = setContextOp(ctx, emq.ctx, ent.OpQueryIDs)
	if err = emq.Select(examinemedication.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (emq *ExamineMedicationQuery) IDsX(ctx context.Context) []int {
	ids, err := emq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (emq *ExamineMedicationQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, emq.ctx, ent.OpQueryCount)
	if err := emq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, emq, querierCount[*ExamineMedicationQuery](), emq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (emq *ExamineMedicationQuery) CountX(ctx context.Context) int {
	count, err := emq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (emq *ExamineMedicationQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, emq.ctx, ent.OpQueryExist)
	switch _, err := emq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (emq *ExamineMedicationQuery) ExistX(ctx context.Context) bool {
	exist, err := emq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ExamineMedicationQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (emq *ExamineMedicationQuery) Clone() *ExamineMedicationQuery {
	if emq == nil {
		return nil
	}
	return &ExamineMedicationQuery{
		config:     emq.config,
		ctx:        emq.ctx.Clone(),
		order:      append([]examinemedication.OrderOption{}, emq.order...),
		inters:     append([]Interceptor{}, emq.inters...),
		predicates: append([]predicate.ExamineMedication{}, emq.predicates...),
		// clone intermediate query.
		sql:  emq.sql.Clone(),
		path: emq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
func (emq *ExamineMedicationQuery) GroupBy(field string, fields ...string) *ExamineMedicationGroupBy {
	emq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ExamineMedicationGroupBy{build: emq}
	grbuild.flds = &emq.ctx.Fields
	grbuild.label = examinemedication.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
func (emq *ExamineMedicationQuery) Select(fields ...string) *ExamineMedicationSelect {
	emq.ctx.Fields = append(emq.ctx.Fields, fields...)
	sbuild := &ExamineMedicationSelect{ExamineMedicationQuery: emq}
	sbuild.label = examinemedication.Label
	sbuild.flds, sbuild.scan = &emq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ExamineMedicationSelect configured with the given aggregations.
func (emq *ExamineMedicationQuery) Aggregate(fns ...AggregateFunc) *ExamineMedicationSelect {
	return emq.Select().Aggregate(fns...)
}

func (emq *ExamineMedicationQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range emq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, emq); err != nil {
				return err
			}
		}
	}
	for _, f := range emq.ctx.Fields {
		if !examinemedication.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if emq.path != nil {
		prev, err := emq.path(ctx)
		if err != nil {
			return err
		}
		emq.sql = prev
	}
	return nil
}

func (emq *ExamineMedicationQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ExamineMedication, error) {
	var (
		nodes = []*ExamineMedication{}
		_spec = emq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ExamineMedication).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ExamineMedication{config: emq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, emq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (emq *ExamineMedicationQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := emq.querySpec()
	_spec.Node.Columns = emq.ctx.Fields
	if len(emq.ctx.Fields) > 0 {
		_spec.Unique = emq.ctx.Unique != nil && *emq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, emq.driver, _spec)
}

func (emq *ExamineMedicationQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(examinemedication.Table, examinemedication.Columns, sqlgraph.NewFieldSpec(examinemedication.FieldID, field.TypeInt))
	_spec.From = emq.sql
	if unique := emq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if emq.path != nil {
		_spec.Unique = true
	}
	if fields := emq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, examinemedication.FieldID)
		for i := range fields {
			if fields[i] != examinemedication.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := emq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := emq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := emq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := emq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (emq *ExamineMedicationQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(emq.driver.Dialect())
	t1 := builder.Table(examinemedication.Table)
	columns := emq.ctx.Fields
	if len(columns) == 0 {
		columns = examinemedication.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if emq.sql != nil {
		selector = emq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if emq.ctx.Unique != nil && *emq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range emq.predicates {
		p(selector)
	}
	for _, p := range emq.order {
		p(selector)
	}
	if offset := emq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := emq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ExamineMedicationGroupBy is the group-by builder for ExamineMedication entities.
type ExamineMedicationGroupBy struct {
	selector
	build *ExamineMedicationQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (emgb *ExamineMedicationGroupBy) Aggregate(fns ...AggregateFunc) *ExamineMedicationGroupBy {
	emgb.fns = append(emgb.fns, fns...)
	return emgb
}

// Scan applies the selector query and scans the result into the given value.
func (emgb *ExamineMedicationGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, emgb.build.ctx, ent.OpQueryGroupBy)
	if err := emgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ExamineMedicationQuery, *ExamineMedicationGroupBy](ctx, emgb.build, emgb, emgb.build.inters, v)
}

func (emgb *ExamineMedicationGroupBy) sqlScan(ctx context.Context, root *ExamineMedicationQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(emgb.fns))
	for _, fn := range emgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*emgb.flds)+len(emgb.fns))
		for _, f := range *emgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*emgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := emgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ExamineMedicationSelect is the builder for selecting fields of ExamineMedication entities.
type ExamineMedicationSelect struct {
	*ExamineMedicationQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ems *ExamineMedicationSelect) Aggregate(fns ...AggregateFunc) *ExamineMedicationSelect {
	ems.fns = append(ems.fns, fns...)
	return ems
}

// Scan applies the selector query and scans the result into the given value.
func (ems *ExamineMedicationSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ems.ctx, ent.OpQuerySelect)
	if err := ems.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ExamineMedicationQuery, *ExamineMedicationSelect](ctx, ems.ExamineMedicationQuery, ems, ems.inters, v)
}

func (ems *ExamineMedicationSelect) sqlScan(ctx context.Context, root *ExamineMedicationQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ems.fns))
	for _, fn := range ems.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ems.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ems.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
